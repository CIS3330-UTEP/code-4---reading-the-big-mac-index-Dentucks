import csv
import pandas as pd
big_mac_file = './big-mac-full-index.csv'

def get_new_country_name(row):
    new_country_name =f"{row['name']}({row['iso_a3']})"
    return new_country_name
    print(get_new_country_name)

df['new_name'] = df.apply(get_new_country_name,axis=1)
print(df['new_name'])

